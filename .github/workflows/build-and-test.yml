name: Build and Test Application

on:
  push:
  pull_request:
    branches:
      - 'main'

jobs:
  test-app:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build the Docker image for testing
        run: docker build -t myapp-test:latest .

      - name: Create dummy password file for testing
        run: echo "testpassword" > postgres_password.txt

      - name: Run services with Docker Compose
        run: docker-compose up --build -d

      - name: Wait for services to be ready
        run: sleep 15

      - name: Test the application endpoint
        run: curl --fail http://localhost:8080/books
